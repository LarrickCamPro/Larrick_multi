# Items

`Docs/items` is reserved for curated or derived item-level artifacts.

Rules:
- Do not duplicate full source bundles from `Docs/sources`.
- Keep source material canonical in `Docs/sources/<bundle>/marker/*`.
- Store only item-specific outputs, annotations, or summaries here.
