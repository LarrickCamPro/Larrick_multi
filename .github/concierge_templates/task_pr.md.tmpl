## Summary

- Workflow: `${workflow}`
- Task branch: `${branch}`
- Base branch: `${base_branch}`
- Promotion branch: `${promotion_branch}`
- Topic: `${topic_slug}`

## Owned Path Scope

${owned_paths_bullets}

## Issue

${issue_line}

## Promotion

After this task lands on `${base_branch}`, open or update the promotion PR from `${base_branch}` to `${promotion_branch}`.

${task_metadata_block}
