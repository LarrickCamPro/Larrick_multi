## Summary

Promote `${base_branch}` into `${promotion_branch}` for the `${workflow}` workflow.

## Linked Task PRs

${linked_task_prs_bullets}

## Commit Delta

${commit_delta_bullets}

## Release Notes Draft

${release_notes_bullets}
