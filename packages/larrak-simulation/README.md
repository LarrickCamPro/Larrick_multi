# larrak-simulation (vendored)

This package wraps the vendored `larrak-simulation` submodule and exposes its Python
package(s) and CLI entry points inside the monorepo.

The source of truth lives under:

- `larrak-simulation/` (git submodule)

